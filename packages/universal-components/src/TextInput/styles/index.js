// @flow

import { defaultTokens } from '@kiwicom/orbit-design-tokens';

export const fontSize = {
  small: parseFloat(defaultTokens.fontSizeInputSmall),
  normal: parseFloat(defaultTokens.fontSizeInputNormal),
};

export const height = {
  small: parseFloat(defaultTokens.heightInputSmall),
  normal: parseFloat(defaultTokens.heightInputNormal),
};
