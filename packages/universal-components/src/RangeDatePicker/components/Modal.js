// @flow

import * as React from 'react';

import { Modal as UCModal } from '../../Modal';
import type { Props } from '../../Modal/ModalTypes';

export default function Modal(props: Props) {
  return <UCModal {...props} />;
}
