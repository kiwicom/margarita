// @flow

import * as React from 'react';
import { ShareBooking } from '@kiwicom/margarita-core';

import MMBWrapper from '../../components/Wrapper';

const ShareBookingScreen = () => (
  <MMBWrapper>
    <ShareBooking />
  </MMBWrapper>
);

export default ShareBookingScreen;
