// @flow

import * as React from 'react';
import { FlightServices } from '@kiwicom/margarita-core';

import MMBWrapper from '../../components/Wrapper';

const FlightServicesScreen = () => (
  <MMBWrapper>
    <FlightServices />
  </MMBWrapper>
);

export default FlightServicesScreen;
