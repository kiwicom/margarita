// @flow

import * as React from 'react';
import { TripServices } from '@kiwicom/margarita-core';

import MMBWrapper from '../../components/Wrapper';

const TripServicesScreen = () => (
  <MMBWrapper>
    <TripServices />
  </MMBWrapper>
);

export default TripServicesScreen;
