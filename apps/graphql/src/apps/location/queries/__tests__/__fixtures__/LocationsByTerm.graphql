{
  locationsByTerm(input: { term: "Prague" }) {
    edges {
      node {
        id
        slug
        name
        countryFlagURL
        city {
          id
          name
        }
        coordinates {
          lat
          lng
        }
      }
    }
  }
}
