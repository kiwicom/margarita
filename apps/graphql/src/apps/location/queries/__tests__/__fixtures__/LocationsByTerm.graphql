{
  locationsByTerm(input: { term: "Prague" }) {
    edges {
      node {
        id
        slug
        name
        countryFlagURL
        type
        city {
          id
          name
        }
        coordinates {
          lat
          lng
        }
      }
    }
  }
}
